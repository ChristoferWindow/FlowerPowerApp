<div id="body-container">
    <div id="flowers_panel">
        <% robots.forEach( robot => { %>
            <div id="plant_<%= robot.id %>">
                <img src="<%= robot.image %>"><br/>
                Nazwa: <%= robot.name %><br/>
                Typ: <%= robot.location %>
                <hr>
            </div>
        <% }) %>
    </div>
</div>